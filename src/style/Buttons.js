import { Button, styled } from "@mui/material";

export const Years = styled(Button)(({ theme }) => ({
    fontSize: "12px",
    minWidth: "70px",
    padding: "2px 8px",
    whiteSpace: "nowrap",
    borderRadius: "999px",
}));